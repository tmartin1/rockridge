# Customize the test machine
machine:
  # Version of node to use.
  node:
    version: 0.10.*

# Customize dependencies
dependencies:
  pre:
    - npm install -g karma
    - npm install -g bower
    - bower install
