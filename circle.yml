# Customize the test machine
machine:

  node:
    version: 0.10.*
    
  environment:
    NODE_ENV: 'production'

# Customize dependencies
dependencies:
  pre:
    - npm install -g bower
    - bower install
