// For directives used within my-plan.

'use strict';

angular.module('rockridge')

// Directive to add item to potential list inputs (assets, liabilities, children, etc.)
.directive('addItem', function() {
  //
});
